import { Box } from "@mui/material";
import React from "react";
import { colors } from "../constants";

const Navbar = () => {
  return (
    <Box sx={{ backgroundColor: colors.MainDarkColor }}>Navbar Component</Box>
  );
};

export default Navbar;
